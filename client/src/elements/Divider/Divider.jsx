import { Divider as CustomDivider } from "antd";
import React from "react";

const Divider = (props) => {
  return (
    <>
      <CustomDivider {...props} />
    </>
  );
};

export default Divider;
